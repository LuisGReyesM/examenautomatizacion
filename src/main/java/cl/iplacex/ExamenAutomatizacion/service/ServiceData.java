package cl.iplacex.ExamenAutomatizacion.service;

public class ServiceData {
    /**
     * Simula la obtención de un valor desde una fuente externa (ej. base de datos, API).
     * @return un valor entero fijo para el test.
     */
    public int obtenerValor() {
        return 10;
    }
}
